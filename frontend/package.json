{
  "name": "retailmind-frontend",
  "version": "0.0.1",
  "private": true,
  "description": "RetailMind AI frontend — Next.js (activated in Sprint S3; UX design doc is normative)",
  "scripts": {
    "dev": "echo 'TODO(S3): next dev' && exit 1",
    "build": "echo 'TODO(S3): next build' && exit 1",
    "lint": "echo 'TODO(S3): eslint' && exit 0",
    "test": "echo 'TODO(S3): vitest' && exit 0"
  }
}
