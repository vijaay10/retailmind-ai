---
# Dagster workspace configuration for RetailMind data platform
#
# This file tells Dagster where to find pipeline definitions.
#
# Usage:
#   dagster dev -w orchestration/workspace.yaml

load_from:
  - python_module:
      module_name: orchestration.dagster
      working_directory: .
      location_name: retailmind_data_platform
