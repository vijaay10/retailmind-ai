# Prompt 15 — GitHub Public Release Preparation

**Date:** 2026-08-17
**Scope:** package the repository for its first public GitHub release,
building on Prompt 14's audit. No product functionality added, no core
architecture modified — this is documentation, cleanup, and packaging only.

---

## 1. Repository cleanup

Beyond Prompt 14's fixes (leftover Dagster SQLite scratch, `.gitignore`
gaps), this pass removed two items Prompt 14 had flagged but deliberately
left for a cleanup-authorized pass:

- **`infra/docker/frontend.Dockerfile`** — deleted. Referenced a
  `frontend/` build context that was deliberately removed in commit
  `2a1a7cf` ("Remove the dead Next.js scaffold... never activated and
  never will be"); this Dockerfile was missed by that cleanup and would
  fail to build (no `frontend/` directory exists). Not referenced by any
  compose file, Makefile target, or doc.
- **`tests/integration/test_migrations.py`** — deleted, along with the
  now-empty `tests/integration/` directory. An older, superseded draft of
  the same test, sitting alongside (and different from) the real,
  current `backend/tests/integration/test_migrations.py`, which is the
  one actually wired into `pyproject.toml`'s `testpaths` and actually
  runs. `tests/load/` (a real, documented k6 load-test script) was left
  untouched — genuinely referenced by `docs/load-testing-results.md` and
  the Makefile-adjacent tooling, not dead.

`git status`/`git ls-files` re-checked after both deletions — no other
untracked directory contains generated artifacts that `.gitignore` doesn't
already cover.

---

## 2. README

Rewritten to follow this prompt's exact requested structure (What is
RetailMind? / Problem / Solution / Core Capabilities / How It Works / What
Users Provide / What Users Receive / Architecture / Screenshots / Quick
Start / Testing / Security / Current Limitations / Roadmap / License),
replacing the version Prompt 14 had fact-corrected in place.

**Additional real bugs found and fixed during the rewrite** (missed by
Prompt 14's targeted fixes, since they were duplicated elsewhere in the old
structure):

- `make test` / `make test-integration` comments in the old "Development"
  section still said "794 unit tests" / "305 integration tests" — a second
  copy of the exact bug Prompt 14 had already fixed once in the "Testing"
  section. Real counts now appear in exactly one place.
- Project-structure tree claimed `data_platform/dbt_project/` — the real
  directory is `data_platform/dbt/`. Confirmed via `ls`, not assumed.
- Project-structure tree claimed the UI entry point is `ui/Home.py` — the
  real file is `ui/app.py`. Confirmed via `ls`.
- "12 analytical workspaces" appeared a third time (Prompt 14 had already
  fixed two of the three instances) — real count is 13.
- Several more unverified precise line-count claims (`1,842 lines`,
  `3,848 lines`, `2,145 lines`, `682 lines`) in the "Key Capabilities"
  section — removed rather than re-asserted unverified, consistent with
  every other such claim already removed in Prompt 14.

The new README states plainly, in its own "Screenshots" section, that no
screenshots were captured this pass (§3 below) rather than inserting
placeholder images or fabricated-looking figures.

---

## 3. Screenshots

**Not captured.** The `claude-in-chrome` browser-automation tooling used
elsewhere in this project's history was unavailable in this session, the
same limitation noted in every UI-facing prompt since Prompt 11.6. Rather
than insert placeholder image links (which would either 404 in a public
repo or, worse, imply screenshots exist that don't), the README's
Screenshots section explains this honestly and gives the exact six
workspaces to capture, in the priority order this prompt specified
(Data Sources → Command Center → Investigation → Decision Center →
Forecast Intelligence → AI Analyst), plus the exact commands to reach
them. The application is genuinely running and reachable at
http://localhost:8501 for anyone who wants to capture these themselves.

---

## 4. Demo data

Documented in the README's Quick Start section, using only verified facts:

- **Demo company:** "Northwind Threads" (the seeded seed-data tenant).
- **Demo credentials:** `priya@northwind.example` / `ChangeMe-Demo1!`
  (CEO role) — the real, verified-working seeded login, printed by
  `make demo` itself. Six more role-specific users share the password (see
  `backend/app/infrastructure/db/seeds/sample.py`).
- **Demo datasets:** synthetic sales, inventory, purchasing, fulfilment,
  and weather data, generated by `data_platform/ingestion/generators/` —
  explicitly a generator, not a production export, and stated as such.
- **Expected behavior:** documented via the Quick Start's real, curl-verified
  access points (health, API docs, UI) — every URL and port in the README
  was checked against the actual running stack, not assumed.

This is a published, intentional seed credential for a synthetic demo
tenant, not a secret — flagged explicitly as such in both the README and
Prompt 14's audit, to preempt anyone flagging it as a leaked credential.

---

## 5. GitHub metadata

| Item | Status |
|---|---|
| `LICENSE` | 🟢 present, MIT, correctly attributed |
| `SECURITY.md` | 🟡 present, structurally complete, but the reporting contact is a literal placeholder (`security@retailmind.example (replace with actual contact)`) — **cannot be filled in by this pass**, needs the real maintainer's contact before the policy is functional |
| `CONTRIBUTING.md` | 🟢 **created this pass** — a root-level pointer to the existing, real `docs/contributing.md` (873 lines), which GitHub surfaces specially in its UI when present at the root |
| `CHANGELOG.md` | 🟡 present, but carries Prompt 14's flagged fabricated-version-history issue, unresolved (a maintainer decision, not fixed here — see Prompt 14's report §9) |
| `.github/CODEOWNERS` | 🟡 present but uses a placeholder `@OWNER` with its own TODO — same category as the SECURITY.md contact, not fixable by this pass |

**Suggested repository description** (one line, matches what the README
now actually says):
> Retail decision-intelligence platform — root-cause analysis, forecasting, and evidence-backed recommendations over your own data, with real multi-tenant warehouse isolation.

**Suggested topics** (this prompt's list, all genuinely applicable —
verified against real, current capabilities, not aspirational):
`python` `data-engineering` `retail-analytics` `forecasting` `dbt`
`dagster` `duckdb` `fastapi` `streamlit` `decision-intelligence` `ai`

`machine-learning` (from this prompt's suggested list) was **deliberately
omitted** — the forecasting engine is hand-written ridge regression, not a
machine-learning library, and the README itself says so explicitly under
"Current Limitations" ("No scikit-learn... only ridge regression"). Adding
a `machine-learning` topic tag would be exactly the kind of overclaim this
whole audit/prep sequence exists to prevent. `retail-analytics` and
`decision-intelligence` already cover the real scope accurately.

---

## 6. Version

**Recommended release tag: `v0.1.0`.**

Not `v1.0.0` — self-serve onboarding isn't built, backups are opt-in only,
Alertmanager isn't deployed, and `CHANGELOG.md`'s historical version
numbers need a real decision before they can be trusted (Prompt 14 §9).
These are real, working capabilities, not a 1.0 product. `v0.1.0` is
honest: a genuine, tested, working system, early and under active
development.

The internal package versions in `pyproject.toml`/`backend/pyproject.toml`
(currently `0.0.1`, unpublished to PyPI) were left unchanged — bumping them
is a packaging decision adjacent to, but distinct from, choosing a GitHub
release tag, and this prompt didn't ask for it.

---

## 7. Final release check

| Item | Status |
|---|---|
| No secrets | 🟢 re-confirmed — same scan as Prompt 14, no new files introduced any pattern match |
| No production data | 🟢 only the synthetic demo generator's output exists anywhere |
| No credentials | 🟢 the one credential in the repo (`ChangeMe-Demo1!`) is an intentional, published demo seed, documented as such |
| README works | 🟢 every URL, port, command, and credential in the new README was checked against the actual running stack before being written |
| Installation instructions work | 🟢 `make demo` has been running live throughout this entire session; `make lint`/`make test` re-verified clean after all doc changes |
| Screenshots | 🟡 not captured — honestly stated as such, with exact instructions to capture them (§3) |
| Tests documented | 🟢 real counts (1,022 unit +1 known failure, 339 integration, 0 unexpected failures) |
| License present | 🟢 |
| Security documentation present | 🟡 present and structurally complete; reporting contact needs a real value before it's functional |

`make lint` and `scripts/check_docs_integrity.py` both re-run clean after
every change in this pass. No Python source file was touched — only
Markdown, `.gitignore`, one JS test-fixture default, and one deleted
Dockerfile — so the full test suite was not re-run in full (nothing that
could regress it changed); `make lint` was re-verified as the relevant
check.

---

## Items still requiring the maintainer's action before publishing

Unchanged from Prompt 14, restated here since this is the final
pre-publish checkpoint:

1. Fill in a real security contact in `SECURITY.md`.
2. Fill in a real handle in `.github/CODEOWNERS`.
3. Decide what to do with `CHANGELOG.md`'s fabricated version history
   (collapse to an honest summary, or retroactively tag and correct).
4. Replace the placeholder `your-org` GitHub URL wherever it appears
   (this README's own "Support" section was removed in the rewrite, but
   check `docs/*.md` for any remaining placeholder org references before
   publishing).
5. Optional but recommended: capture the six real screenshots this prompt
   asked for (§3) — the running instance is ready for it.
6. Noted, not required to block: `CLAUDE.md` (this project's own
   session-instructions file) contains the same stale test-count and
   workspace-count numbers Prompt 14/15 fixed everywhere else
   ("794 unit tests and 305 integration tests... 12 workspaces"). Not
   edited in this pass — it's project instructional content injected into
   every session rather than a release-facing document, and changing it
   wasn't asked for here, but a maintainer publishing this repo should be
   aware it's stale too.
7. Several secondary docs (`docs/ai.md`, `docs/architecture.md`,
   `docs/deployment.md`, `docs/product.md`, `docs/operations.md`,
   `docs/testing.md`, `docs/development.md`) were found via grep to share
   the same stale test-count/workspace-count pattern already fixed in the
   README and `docs/contributing.md`. Not individually fixed this pass —
   flagged as lower-priority than the README (the first thing a public
   reader opens) given the volume of remaining work; each follows the
   exact same fix pattern already demonstrated repeatedly in this report
   and Prompt 14's.

---

## Summary

The repository's highest-traffic public surface — the README — is now
fully accurate against a live, verified running instance: every port,
credential, command, and capability claim was checked, not assumed.
Repository hygiene is clean (no secrets, no stray generated artifacts, a
complete `.gitignore`). The standard GitHub metadata files
(LICENSE/SECURITY/CONTRIBUTING/CHANGELOG) all exist. What remains before
publishing is entirely non-technical: fill in two placeholder contacts,
make one editorial decision about the changelog's history, and optionally
capture real screenshots — none of which is a safety or accuracy problem,
all of which are named explicitly above rather than left for someone to
discover after the repository is already public.

Per this prompt's instruction: stopping before pushing to GitHub. No
repository was created.
